{{ log_path }} {
    daily
    dateext
    dateformat -%Y%m%d
    rotate 7
    compress
    notifempty
    missingok
    sharedscripts
    postrotate
    /bin/kill -USR1 `cat {{ nginx_path }}/logs/nginx.pid  2>/dev/null` 2>/dev/null || true
    endscript
}
